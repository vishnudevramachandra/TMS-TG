import matplotlib.pyplot as plt

def raster_plot(**kwargs):
    rows = {'raster', 'psfr'}
    keys = kwargs.keys()

    for key in kwargs:
        if key == 'raster':
            rastt


def plot(raster=None, psfr=None, colNames=None):

    if raster is None and psfr is None:
        pass
    elif raster is not None and psfr is None:
        for i in len(raster):

    elif psfr is not None and raster is None:
        ...
    else:
        ...
